# Raw API snapshots

Verbatim `GET /plans/{id}` responses from the YNAB reference account, kept so every
behavior doc can be re-derived and audited. Named `<date>-sk<server_knowledge>-<endpoint>.json`.

Disposable test data only. No production or personal financial data belongs here.
